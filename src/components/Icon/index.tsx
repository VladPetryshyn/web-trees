"use client"

import { FontAwesomeIcon, FontAwesomeIconProps } from "@fortawesome/react-fontawesome"
import { FC } from "react"

export const Icon: FC<FontAwesomeIconProps> = (props) => <FontAwesomeIcon {...props} fixedWidth />
